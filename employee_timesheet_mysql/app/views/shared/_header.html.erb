<div class="logoDiv"><%= image_tag("sample2.png", :class => "mainLogo") %></div>
<% if controller_name.eql?('sessions') && action_name.eql?('index') || controller_name.eql?('users') && action_name.eql?('new') %>
  <div>
    <h2>
      <p class="loginHeader">
        Time-sheet Application
      </p>
    </h2>
  </div>
<% else %>
  <div class="headerTextContaint">
    <h4 class="mainNameText"> Hi, <%= session[:user].user_name.capitalize %> </h4>
    <h2><p class="mainHeaderText"> Welcome to Time-sheet Application</p></h2>
    <div class="logoutDiv">
      <% unless session[:user][:designation_id].eql?(1) %>
        <%= link_to 'Employees timesheets', users_path, :class => 'menuOptios'%>
      <% end %>
      <%= link_to 'Previous time-sheets status', statuses_path, :class => 'menuOptios' %>
      <%= link_to 'Todays time-sheet', timesheets_path, :class => 'menuOptios'%>
      <%= link_to "Logout", session_path(session[:user]), :method => 'delete' %></div>
  </div>
<% end %>